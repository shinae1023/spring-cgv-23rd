services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: ceos-app
    container_name: ceos-app
    env_file:
      - .env
    ports:
      - "8080:8080"
    restart: unless-stopped
